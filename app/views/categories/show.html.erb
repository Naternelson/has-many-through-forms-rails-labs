<h1><%= @category.name.titlecase %></h1>
<div></div>
<div>
    <h2>All Posts</h2>
    <ul>
        <% @category.posts.each do |p| %>
            <li><%= link_to p.title, post_path(p) %></li>
        <% end %>
    <ul>
</div>